# 🚀 宁夏文旅小程序 - 前后端连接完整指南

## 📋 目录
1. [数据库配置](#1-数据库配置)
2. [后端配置](#2-后端配置)
3. [启动后端服务](#3-启动后端服务)
4. [前端配置](#4-前端配置)
5. [启动前端项目](#5-启动前端项目)
6. [测试连接](#6-测试连接)
7. [常见问题](#7-常见问题)

---

## 1. 数据库配置

### 1.1 安装 MySQL

#### Windows 下载安装
1. 访问：https://dev.mysql.com/downloads/mysql/
2. 下载 MySQL Installer
3. 安装时记住设置的 **root 密码**
4. 确保服务运行在 **3306** 端口

#### 验证 MySQL 安装
```bash
# 打开命令行，连接 MySQL
mysql -u root -p
# 输入密码后，看到 MySQL> 提示符即为成功
```

### 1.2 创建数据库

#### 方式一：使用命令行
```bash
# 登录 MySQL
mysql -u root -p

# 创建数据库
CREATE DATABASE ningxia_wenlv CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 退出
EXIT;
```

#### 方式二：使用 Navicat（图形化工具）
1. 打开 Navicat
2. 连接到 MySQL（root + 密码）
3. 右键 → 新建数据库
4. 名称：`ningxia_wenlv`
5. 字符集：`utf8mb4`

### 1.3 数据库配置文件

后端项目使用 `.env` 文件存储数据库配置，位置：
```
C:\Users\wzq33\宁夏文旅小程序\backend\.env
```

**当前配置：**
```env
# 服务器配置
SERVER_PORT=8080
SERVER_MODE=debug

# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=123456Abc
DB_NAME=ningxia_wenlv

# JWT 配置
JWT_SECRET=default-secret-change-this
JWT_EXPIRE_HOURS=24

# 文件上传配置
UPLOAD_PATH=./static/uploads
MAX_FILE_SIZE=10485760

# 跨域配置
CORS_ALLOW_ORIGINS=*
```

**⚠️ 重要提示：**
- 如果你的 MySQL 密码不是 `123456Abc`，请修改 `.env` 文件
- `.env` 文件已配置好，**一般不需要修改**

### 1.4 初始化数据库

后端会自动创建表结构，只需导入测试数据。

#### 方式一：使用后端数据导入工具
```bash
cd C:\Users\wzq33\宁夏文旅小程序\backend\tools\import2

# 运行导入程序
go run main.go
```

#### 方式二：使用已有数据
项目已配置好，无需手动导入。后端启动时会自动连接数据库。

---

## 2. 后端配置

### 2.1 后端目录结构
```
backend/
├── main.go              # 主程序入口
├── .env                 # 配置文件（数据库连接等）
├── config/              # 配置包
│   └── config.go
├── db/                 # 数据库连接
│   └── database.go
├── api/v1/             # API 接口
├── models/             # 数据模型
├── utils/             # 工具函数
└── static/            # 静态文件（图片上传）
```

### 2.2 数据库连接代码

**文件：** `backend/db/database.go`

```go
package db

import (
    "fmt"
    "log"
    "ningxia-wenlv-backend/config"
    "ningxia-wenlv-backend/models"

    "gorm.io/driver/mysql"
    "gorm.io/gorm"
)

var DB *gorm.DB  // 全局数据库连接对象

func Connect() error {
    cfg := config.GlobalConfig

    // 构建 DSN（Data Source Name）
    dsn := fmt.Sprintf("%s:%s@tcp(%s:%s)/%s?charset=utf8mb4&parseTime=True&loc=Local",
        cfg.DBUser,
        cfg.DBPassword,
        cfg.DBHost,
        cfg.DBPort,
        cfg.DBName,
    )

    // 连接数据库
    var err error
    DB, err = gorm.Open(mysql.Open(dsn), &gorm.Config{})
    if err != nil {
        return fmt.Errorf("failed to connect database: %v", err)
    }

    // 自动迁移表结构（自动创建表）
    err = DB.AutoMigrate(
        &models.User{},
        &models.Attraction{},
        &models.Food{},
        &models.Culture{},
        &models.Product{},
        &models.Order{},
        &models.Cart{},
        &models.Coupon{},
        &models.Admin{},
    )

    if err != nil {
        return fmt.Errorf("failed to auto migrate: %v", err)
    }

    log.Println("Database connected successfully")
    return nil
}
```

**📝 说明：**
- `gorm.Open()`：连接数据库
- `AutoMigrate()`：自动创建表结构（如果表不存在）
- 不需要手动创建表！

---

## 3. 启动后端服务

### 3.1 安装 Go 环境

#### 检查 Go 是否安装
```bash
go version
# 显示版本号即为安装成功
```

#### 如未安装 Go
1. 访问：https://golang.org/dl/
2. 下载 Windows 安装包
3. 安装到默认路径

### 3.2 启动后端

#### 方式一：使用命令行（推荐）
```bash
# 进入后端目录
cd C:\Users\wzq33\宁夏文旅小程序\backend

# 启动服务
go run main.go
```

**启动成功标志：**
```
Database connected successfully
[GIN-debug] Listening and serving HTTP on :8080
```

#### 方式二：编译后运行
```bash
# 编译
cd C:\Users\wzq33\宁夏文旅小程序\backend
go build -o server.exe main.go

# 运行
./server.exe
```

### 3.3 验证后端运行

打开浏览器访问：
```
http://localhost:8080
```

应看到：
```
{"code": 200, "message": "success", "data": "宁夏文旅小程序后端 API"}
```

测试 API 接口：
```bash
# 测试景点列表
curl http://localhost:8080/api/v1/attractions
```

### 3.4 常见问题

#### 问题：端口被占用
```
Error: listen tcp :8080: bind: address already in use
```

**解决方法：**
```bash
# 查找占用端口的进程
netstat -ano | findstr :8080

# 杀死进程（使用 PID）
taskkill /PID <进程ID> /F

# 或修改 .env 文件中的 SERVER_PORT=8081
```

#### 问题：数据库连接失败
```
Error: failed to connect database
```

**解决方法：**
1. 检查 MySQL 是否启动
2. 检查 `.env` 文件中的数据库配置是否正确
3. 检查 MySQL 密码

---

## 4. 前端配置

### 4.1 前端目录结构
```
宁夏文旅小程序/
├── pages/              # 页面
│   ├── index/          # 首页
│   ├── profile/        # 我的
│   ├── market/         # 商城
│   ├── attractions/    # 景点
│   ├── food/           # 美食
│   └── culture/        # 文化
├── utils/             # 工具函数
│   ├── api.js         # API 请求封装
│   └── request.js     # 接口统一管理
└── app.js             # 小程序入口
```

### 4.2 前端 API 配置

**文件：** `utils/api.js`

```javascript
// API配置
const config = {
  baseURL: 'http://localhost:8080',  // ⚠️ 后端地址
  apiVersion: '/api/v1'            // API 版本
}
```

**📝 说明：**
- `baseURL`：后端服务器地址
- 开发环境：`http://localhost:8080`
- 生产环境：换成实际服务器地址（如 `https://api.yourdomain.com`）

### 4.3 请求封装

**文件：** `utils/api.js`

```javascript
function request(options) {
  const token = wx.getStorageSync('token')

  return new Promise((resolve, reject) => {
    wx.request({
      url: config.baseURL + config.apiVersion + options.url,
      method: options.method || 'GET',
      data: options.data || {},
      header: {
        'Content-Type': 'application/json',
        'Authorization': token ? `Bearer ${token}` : '',
        ...options.header
      },
      success: (res) => {
        // 处理响应
        if (res.statusCode === 200) {
          if (res.data.code === 200) {
            resolve(res.data.data)
          } else {
            wx.showToast({
              title: res.data.message || '请求失败',
              icon: 'none'
            })
            reject(res.data)
          }
        }
      },
      fail: (err) => {
        wx.showToast({
          title: '网络连接失败',
          icon: 'none'
        })
        reject(err)
      }
    })
  })
}
```

**📝 说明：**
- 自动添加 `Authorization` 头部（如果有 token）
- 统一错误处理
- 自动显示 Toast 提示

### 4.4 接口定义

**文件：** `utils/request.js`

```javascript
const API = {
  // 登录
  login: (data) => api.post('/auth/login', data),

  // 景点
  getAttractions: (params) => api.get('/attractions', params),
  getAttraction: (id) => api.get(`/attractions/${id}`),

  // 美食
  getFoods: (params) => api.get('/food', params),
  getFood: (id) => api.get(`/food/${id}`),

  // 商品
  getProducts: (params) => api.get('/market', params),
  getProduct: (id) => api.get(`/market/${id}`),

  // 购物车
  getCart: () => api.get('/user/cart'),
  addToCart: (data) => api.post('/user/cart', data),
}
```

---

## 5. 启动前端项目

### 5.1 安装微信开发者工具

1. 访问：https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html
2. 下载 Windows 版本
3. 安装后使用微信扫码登录

### 5.2 导入项目

1. 打开微信开发者工具
2. 点击 **"+"** 导入项目
3. 选择目录：
   ```
   C:\Users\wzq33\宁夏文旅小程序
   ```
4. AppID：选择 **测试号** 或 **自己的 AppID**
5. 项目名称：宁夏文旅小程序
6. 点击 **导入**

### 5.3 配置不校验域名

**开发阶段需要关闭域名校验：**

1. 微信开发者工具 → 右上角 **"详情"**
2. **"本地设置"** → 勾选 **"不校验合法域名、web-view（业务域名）、TLS 版本以及 HTTPS 证书"**

### 5.4 启动流程

**完整启动步骤：**

```bash
# 终端 1：启动 MySQL（如未自动启动）
# Windows 服务管理器中确保 MySQL 服务运行

# 终端 2：启动后端
cd C:\Users\wzq33\宁夏文旅小程序\backend
go run main.go

# 终端 3：启动微信开发者工具
# 打开项目，点击"编译"
```

### 5.5 调试面板

在微信开发者工具中：

1. **Console 面板**：查看日志输出
   ```javascript
   console.log('调试信息')
   ```

2. **Network 面板**：查看网络请求
   - 查看请求地址、参数、响应

3. **Storage 面板**：查看本地存储
   ```javascript
   wx.setStorageSync('token', 'xxx')
   wx.getStorageSync('token')
   ```

---

## 6. 测试连接

### 6.1 完整测试流程

#### 步骤 1：启动后端
```bash
cd C:\Users\wzq33\宁夏文旅小程序\backend
go run main.go
```

看到：
```
Database connected successfully
[GIN-debug] Listening and serving HTTP on :8080
```

#### 步骤 2：启动微信开发者工具
1. 导入项目
2. 勾选"不校验合法域名"
3. 点击"编译"

#### 步骤 3：测试 API 接口
在小程序 Console 中输入：
```javascript
// 测试获取景点列表
wx.request({
  url: 'http://localhost:8080/api/v1/attractions',
  method: 'GET',
  success: (res) => {
    console.log('景点列表：', res.data)
  }
})
```

#### 步骤 4：测试登录
1. 进入"我的"页面
2. 点击"点击登录"
3. 检查 Console 是否有登录成功的日志

### 6.2 API 接口列表

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 登录 | POST | `/api/v1/auth/login` | 用户登录 |
| 景点列表 | GET | `/api/v1/attractions` | 获取景点列表 |
| 景点详情 | GET | `/api/v1/attractions/:id` | 获取景点详情 |
| 美食列表 | GET | `/api/v1/food` | 获取美食列表 |
| 文化列表 | GET | `/api/v1/culture` | 获取文化列表 |
| 商品列表 | GET | `/api/v1/market` | 获取商品列表 |
| 商品详情 | GET | `/api/v1/market/:id` | 获取商品详情 |
| 购物车 | GET | `/api/v1/user/cart` | 获取购物车 |
| 加入购物车 | POST | `/api/v1/user/cart` | 添加到购物车 |

---

## 7. 常见问题

### 7.1 数据库相关

#### Q: 如何查看数据库数据？
**A: 使用 Navicat 或命令行**
```bash
mysql -u root -p
use ningxia_wenlv;
SELECT * FROM users;
```

#### Q: 数据库连接失败怎么办？
**A: 检查以下几点**
1. MySQL 是否启动
2. `.env` 文件配置是否正确
3. 用户名和密码是否正确
4. 防火墙是否阻止连接

### 7.2 后端相关

#### Q: 后端启动失败？
**A: 检查以下几点**
1. Go 是否正确安装
2. 端口是否被占用
3. 数据库是否可连接
4. 依赖包是否下载

#### Q: 如何安装 Go 依赖？
**A: 在 backend 目录执行**
```bash
go mod download
```

### 7.3 前端相关

#### Q: 网络请求失败？
**A: 检查以下几点**
1. 后端是否启动
2. 后端地址是否正确（`utils/api.js` 中的 `baseURL`）
3. 是否勾选"不校验合法域名"
4. 防火墙是否阻止

#### Q: 如何查看前端日志？
**A: 在微信开发者工具中**
1. 打开 Console 面板
2. 使用 `console.log()` 输出日志
3. 查看 Network 面板查看请求

#### Q: 登录后 token 存在哪里？
**A: 微信小程序本地存储**
```javascript
// 存储
wx.setStorageSync('token', 'xxx')

// 读取
wx.getStorageSync('token')
```

### 7.4 部署相关

#### Q: 如何部署到生产环境？
**A: 步骤如下**
1. **数据库**：使用云数据库（如阿里云 RDS）
2. **后端**：部署到云服务器（如阿里云 ECS）
3. **前端**：上传到微信后台，使用真实域名

**修改配置：**
```javascript
// utils/api.js
const config = {
  baseURL: 'https://api.yourdomain.com',  // 改为生产地址
  apiVersion: '/api/v1'
}
```

---

## 📞 快速参考

### 常用命令

```bash
# 启动 MySQL（Windows）
net start MySQL

# 停止 MySQL（Windows）
net stop MySQL

# 登录 MySQL
mysql -u root -p

# 启动后端
cd C:\Users\wzq33\宁夏文旅小程序\backend
go run main.go

# 编译后端
go build -o server.exe main.go
```

### 配置文件位置

| 文件 | 位置 | 用途 |
|------|------|------|
| `.env` | `backend/.env` | 数据库、服务器配置 |
| `api.js` | `utils/api.js` | 前端 API 地址配置 |
| `database.go` | `backend/db/database.go` | 数据库连接代码 |

### 端口说明

| 服务 | 端口 | 说明 |
|------|------|------|
| MySQL | 3306 | 数据库默认端口 |
| 后端 API | 8080 | API 服务端口 |

---

## 🎉 总结

### 连接流程
1. ✅ 安装 MySQL
2. ✅ 创建数据库 `ningxia_wenlv`
3. ✅ 配置 `.env` 文件（数据库连接信息）
4. ✅ 启动后端服务（`go run main.go`）
5. ✅ 配置前端 API 地址（`utils/api.js`）
6. ✅ 启动微信开发者工具
7. ✅ 测试连接

### 项目当前状态
- ✅ 数据库：已配置
- ✅ 后端：已配置
- ✅ 前端：已配置
- ✅ 数据导入：已配置
- ✅ 测试数据：已导入

**现在你只需要启动后端和微信开发者工具即可！**

---

## 📚 学习资源

### Go 语言
- Go 官方文档：https://golang.org/doc/
- Gin 框架：https://gin-gonic.com/docs/
- GORM 文档：https://gorm.io/docs/

### 微信小程序
- 微信官方文档：https://developers.weixin.qq.com/miniprogram/dev/framework/
- API 调试工具：微信开发者工具

### 数据库
- MySQL 官方文档：https://dev.mysql.com/doc/
- Navicat（图形化工具）：https://www.navicat.com/

---

**祝你开发顺利！如有问题，请查阅本文档或询问。** 🚀
