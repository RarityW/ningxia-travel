<!-- 宁夏文旅 - 首页 -->
<view class="container">
  <!-- 顶部导航栏 -->
  <view class="nav-bar">
    <view class="location-section">
      <text class="location-icon">📍</text>
      <view class="location-info">
        <text class="location-text">宁夏回族自治区</text>
        <view class="weather-info">
          <text class="weather-icon">{{weatherIcon}}</text>
          <text class="temperature">{{temperature}}</text>
        </view>
      </view>
      <view class="location-arrow">▼</view>
    </view>
    <view class="search-box">
      <text class="search-icon">🔍</text>
      <input class="search-input" placeholder="出行" confirm-type="search" bindconfirm="onSearch" />
    </view>
  </view>

  <!-- 品牌视觉区 -->
  <view class="brand-section">
    <swiper class="brand-swiper" indicator-dots indicator-color="rgba(255,255,255,0.3)" indicator-active-color="#FFFFFF" autoplay circular>
      <swiper-item>
        <view class="brand-slide">
          <image class="brand-bg" src="https://images.unsplash.com/photo-1504280390367-361c6d9f38f4?w=800&h=600&fit=crop" mode="aspectFill" />
          <view class="brand-overlay">
            <view class="brand-content">
              <text class="brand-slogan">旅游来宁夏，给心放个假</text>
              <text class="brand-subtitle">探寻丝路文化，感受大漠风情</text>
            </view>
          </view>
        </view>
      </swiper-item>
      <swiper-item>
        <view class="brand-slide">
          <image class="brand-bg" src="https://images.unsplash.com/photo-1548013146-72479768bada?w=800&h=600&fit=crop" mode="aspectFill" />
          <view class="brand-overlay">
            <view class="brand-content">
              <text class="brand-slogan">神奇宁夏，星星故乡</text>
              <text class="brand-subtitle">大漠黄河·西夏古韵</text>
            </view>
          </view>
        </view>
      </swiper-item>
    </swiper>
  </view>

  <!-- 金刚区图标 (10个) -->
  <view class="diamond-section">
    <view class="diamond-grid">
      <view
        wx:for="{{diamondIcons}}"
        wx:key="id"
        class="diamond-item"
        bindtap="onDiamondTap"
        data-id="{{item.id}}"
      >
        <view class="diamond-icon-wrapper">
          <text class="diamond-emoji">{{item.emoji}}</text>
          <view class="diamond-badge" wx:if="{{item.badge}}">{{item.badge}}</view>
        </view>
        <text class="diamond-name">{{item.name}}</text>
      </view>
    </view>
  </view>

  <!-- 滚动通知栏 -->
  <view class="notice-bar">
    <view class="notice-scroll">
      <text class="notice-item">🔔 2026年优惠活动 · 枸杞免费采摘体验</text>
      <text class="notice-item">🎫 政策优惠 · 景区门票5折起</text>
    </view>
  </view>

  <!-- Banner 区 -->
  <view class="banner-section">
    <swiper class="banner-swiper" indicator-dots autoplay circular>
      <swiper-item wx:for="{{banners}}" wx:key="id">
        <view class="banner-slide" style="background: linear-gradient(135deg, {{item.color1}}, {{item.color2}})">
          <view class="banner-content">
            <text class="banner-title">{{item.title}}</text>
            <text class="banner-desc">{{item.desc}}</text>
            <view class="banner-btn">立即查看</view>
          </view>
        </view>
      </swiper-item>
    </swiper>
  </view>

  <!-- 推荐景点 -->
  <view class="section-card">
    <view class="section-header">
      <text class="section-title">🏔 推荐景点</text>
      <text class="section-more" bindtap="goToAttractions">更多 ></text>
    </view>
    <scroll-view class="hot-scroll" scroll-x>
      <view
        wx:for="{{hotAttractions}}"
        wx:key="id"
        class="hot-card"
        bindtap="goToAttractionDetail"
        data-id="{{item.id}}"
      >
        <image class="hot-image" src="{{item.coverImage}}" mode="aspectFill" />
        <view class="hot-overlay">
          <view class="hot-grade">{{item.grade}}</view>
        </view>
      </view>
    </scroll-view>
  </view>

  <!-- 推荐美食 -->
  <view class="section-card">
    <view class="section-header">
      <text class="section-title">🍜 推荐美食</text>
      <text class="section-more" bindtap="goToFood">更多 ></text>
    </view>
    <scroll-view class="hot-scroll" scroll-x>
      <view
        wx:for="{{hotFood}}"
        wx:key="id"
        class="food-card"
        bindtap="goToFoodDetail"
        data-id="{{item.id}}"
      >
        <image class="food-image" src="{{item.coverImage}}" mode="aspectFill" />
        <view class="food-overlay">
          <text class="food-name">{{item.name}}</text>
          <view class="food-price">¥{{item.price}}</view>
        </view>
      </view>
    </scroll-view>
  </view>

  <!-- 推荐好礼 -->
  <view class="section-card">
    <view class="section-header">
      <text class="section-title">🎁 宁选好礼</text>
      <text class="section-more" bindtap="goToMarket">更多 ></text>
    </view>
    <view class="gift-grid">
      <view
        wx:for="{{gifts}}"
        wx:key="id"
        class="gift-card"
        bindtap="goToGiftDetail"
        data-id="{{item.id}}"
      >
        <image class="gift-image" src="{{item.image}}" mode="aspectFill" />
        <view class="gift-info">
          <text class="gift-name">{{item.name}}</text>
          <view class="gift-meta">
            <text class="gift-tag">{{item.category}}</text>
            <text class="gift-price">¥{{item.price}}</text>
          </view>
        </view>
      </view>
    </view>
  </view>
</view>
