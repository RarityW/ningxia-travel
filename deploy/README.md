# 🚀 Deployment Configuration

This directory contains deployment-related configurations and scripts.

## Structure

- `docker/` - Docker and docker-compose files
- `nginx/` - Nginx reverse proxy configuration
- `scripts/` - Deployment automation scripts

## Usage

```bash
# Build and deploy with Docker
cd deploy/docker
docker-compose up -d

# Configure Nginx
cd deploy/nginx
# Copy configuration files to your server
```
